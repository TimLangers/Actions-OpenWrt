name: OpenWrt-合集版 (最优加速通知完美整合版)

on:
  workflow_dispatch:
  schedule:
    - cron: '0 16 * * *'

env:
  REPO_URL: https://github.com/openwrt/openwrt.git
  REPO_BRANCH: openwrt-25.12
  FEEDS_CONF: feeds.conf.default
  CONFIG_FILE: OK2.config
  DIY_P1_SH: diy-part1.sh
  DIY_P2_SH: diy-part2.sh
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: 1️⃣ 拉取项目分支
      uses: actions/checkout@v4

    - name: 2️⃣ 清理磁盘空间
      uses: jlumbroso/free-disk-space@main
      with:
        tool-cache: true
        android: true
        dotnet: true
        haskell: true
        large-packages: true
        swap-storage: true

    - name: 3️⃣ 查看机器配置与安装 ccache
      id: machine_info
      run: |
        sudo apt-get update
        sudo apt-get install -y ccache jq
        echo "FILE_DATE=$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV
        # (此处保留你原有的 CPU/内存信息打印逻辑...)

    - name: 4️⃣ 安装全量编译环境
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
        sudo docker image prune --all --force
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool lrzsz mkisofs msmtp ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pyelftools python3-setuptools qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev jq
        sudo timedatectl set-timezone "$TZ"

    - name: 5️⃣ 拦截并配置编译缓存
      uses: actions/cache@v4
      with:
        path: |
          /workdir/openwrt/dl
          ~/.ccache
        key: ${{ runner.os }}-openwrt-cache-${{ hashFiles('**/feeds.conf.default', '**/*.config') }}

    - name: 6️⃣ 发送【编译开始】通知
      run: |
        MSG="🚀 <b>OpenWrt 编译开始</b>%0A📅 <b>时间</b>：$(date '+%Y-%m-%d %H:%M:%S')"
        PAYLOAD=$(jq -n --arg chat_id "${{ secrets.TELEGRAM_CHAT_ID }}" --arg text "$MSG" '{chat_id: $chat_id, text: $text, parse_mode: "HTML"}')
        curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" -H "Content-Type: application/json" -d "$PAYLOAD" || true

    - name: 7️⃣ 克隆与 DIY 准备
      run: |
        git clone $REPO_URL -b $REPO_BRANCH openwrt
        [ -e $FEEDS_CONF ] && cp $FEEDS_CONF openwrt/feeds.conf.default
        chmod +x $DIY_P1_SH && ./$DIY_P1_SH
        cd openwrt && ./scripts/feeds update -a && ./scripts/feeds install -a
        [ -e $CONFIG_FILE ] && cp $CONFIG_FILE openwrt/.config
        chmod +x $DIY_P2_SH && ./$DIY_P2_SH
        make defconfig

    - name: 8️⃣ 编译固件
      id: compile
      run: |
        cd openwrt
        make -j$(nproc) || make -j1 V=s
        echo "status=success" >> $GITHUB_OUTPUT

    - name: 9️⃣ 发送【编译结果】通知
      if: always()
      run: |
        STATUS=$([[ "${{ steps.compile.outputs.status }}" == "success" ]] && echo "✅ 成功" || echo "❌ 失败")
        MSG="${STATUS}%0A📅 <b>时间</b>：$(date '+%Y-%m-%d %H:%M:%S')%0A🔗 <a href='${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}'>查看日志</a>"
        PAYLOAD=$(jq -n --arg chat_id "${{ secrets.TELEGRAM_CHAT_ID }}" --arg text "$MSG" '{chat_id: $chat_id, text: $text, parse_mode: "HTML"}')
        curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_TOKEN }}/sendMessage" -H "Content-Type: application/json" -d "$PAYLOAD" || true

    - name: 🔟 上传 Releases
      if: steps.compile.outputs.status == 'success'
      uses: softprops/action-gh-release@v2
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: OpenWrt_${{ env.FILE_DATE }}
        files: openwrt/bin/targets/*/*/*.{img.gz,img,bin}
